[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
# Adiciona opções de cobertura para gerar relatório durante a execução de CI
addopts = --reuse-db --cov=apps --cov-report=term-missing